import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        borderTopWidth: 1,
        borderTopColor: "#333333",
        alignItems: "center",
        paddingTop: 48,
        gap: 12
    },
    icon: {
        color: "#808080"
    },
    title: {
        fontSize: 16,
        fontWeight: "bold",
        color: "#808080"
    },
    subtitle: {
        fontSize: 14,
        color: "#808080"
    }
})